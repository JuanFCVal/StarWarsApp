import 'package:flutter/material.dart';

class GenericAlert extends StatelessWidget {
  String title;
  String body;
  GenericAlert(this.title, this.body);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
